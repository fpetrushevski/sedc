kubectl exec -it curl-default-sa -n foo -c main -- curl localhost:8001/api/v1/pods

kubectl exec -it curl-default-sa -n foo -c main -- curl localhost:8001/api/v1/namespaces/foo/pods

kubectl exec -it curl-default-sa -n foo -c main -- curl localhost:8001/api/v1/namespaces/bar/pods